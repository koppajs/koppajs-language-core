#!/usr/bin/env sh
set -eu

if ! command -v npm >/dev/null 2>&1; then
  echo "npm is required for pre-commit checks." >&2
  exit 1
fi

npm run check:docs
npm run check:meta
npm run format:check
npm run lint
