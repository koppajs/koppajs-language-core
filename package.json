{
  "name": "@koppajs/koppajs-language-core",
  "version": "0.1.3",
  "description": "Shared KoppaJS language primitives, semantic service, and workspace graph.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/koppajs/koppajs-language-core.git"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "prepack": "npm run validate",
    "release:check": "npm pack --dry-run --ignore-scripts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "check": "npm run check:docs && npm run check:meta && npm run format:check && npm run lint && npm run typecheck && npm run test",
    "validate": "npm run check && npm run build",
    "check:docs": "npm run check:docs:contract && npm run check:docs:semantics",
    "check:docs:contract": "node scripts/check-doc-contract.mjs",
    "check:docs:semantics": "node scripts/check-doc-semantics.mjs",
    "check:meta": "node scripts/check-meta-layer.mjs",
    "prepare": "husky"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^22.19.15",
    "@typescript-eslint/eslint-plugin": "^8.48.1",
    "@typescript-eslint/parser": "^8.48.1",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^10.1.8",
    "vitest": "^3.2.4",
    "husky": "^9.1.7",
    "prettier": "^3.7.3"
  },
  "publishConfig": {
    "access": "public"
  },
  "private": false
}
